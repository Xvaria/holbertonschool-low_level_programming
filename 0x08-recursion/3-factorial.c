#include "holberton.h"

/**
 * factorial -  factorial
 * Description: returns the factorial of a given number
 * @n: number
 * Return: -1 or factorial
 */
int factorial(int n)
{
	if(i < 0) {
		return (-1);
	}
	return n * factorial(n - 1);
}
